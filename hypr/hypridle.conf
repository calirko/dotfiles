general {
    lock_cmd = pidof hyprlock >/dev/null || hyprlock
    before_sleep_cmd = pidof hyprlock >/dev/null || hyprlock
    ignore_dbus_inhibit = false
}

listener {
    timeout = 1200
    on-timeout = pidof hyprlock >/dev/null || hyprlock
}
