# portal

# /usr/share/xdg-desktop-portal/hyprland-portals.conf

# monitors
bindl = , switch:Lid Switch, exec, /home/calirko/.config/hypr/scripts/laptop-lid-init.sh

# monitorv2 {
#    output = desc:BOE 0x0A2A
#    mode = 1920x1200@60
#    position = auto-left
#    disabled = true
# }

monitorv2 {
   output = desc:AOC 22B1WG5 AUWMAXA004846
   mode = 1920x1080@75
   position = 0x0
   scale = 1
}

monitorv2 {
   output = desc:Samsung Electric Company LF24T35 HX5X803123
   mode = 1920x1080@75
   position = 0x0
   scale = 1
}

monitorv2 {
   output = desc:Samsung Electric Company LF24T35 HX5X802342
   mode = 1920x1080@75
   position = 1920x-180
   scale = 1
   transform = 3
}

# programs
$terminal = kitty
$fileManager = nautilus
$menu = wofi --style ~/.config/wofi/style.css --allow-images --show drun

# autostart
exec-once = /home/calirko/.config/hypr/scripts/laptop-lid-init.sh
exec-once = systemctl --user start hyprpolkitagent
exec-once = /home/calirko/.config/hypr/scripts/wallpaper-default.sh
exec-once = eww daemon
exec-once = /home/calirko/.config/hypr/scripts/bar.sh
exec-once = hypridle
exec-once = easyeffects --gapplication-service
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
exec-once = bash -lc 'sleep 5; ~/.config/eww/scripts/vpn-menu.sh connect peer_dragon'

# env
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = GDK_BACKEND,wayland
env = ADW_DISABLE_PORTAL,1 
env = XDG_CURRENT_DESKTOP,Hyprland
env = HYPRSHOT_DIR,/home/calirko/Pictures/Screenshots
env = GTK_FONT_NAME,Inter Regular 12
env = GSETTINGS_SCHEMA_DIR,/usr/share/glib-2.0/schemas
env = GTK_THEME,adw-gtk3

# Permissions

# See https://wiki.hypr.land/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow

#####################
### LOOK AND FEEL ###
#####################

general {
    gaps_in = 5
    gaps_out = 10
    border_size = 0
   #  col.active_border = rgba(4c4c4cff)
   #  col.inactive_border = rgba(303030aa)
    resize_on_border = false
    allow_tearing = false
    layout = dwindle
}

decoration {
   #  rounding = 0
   #  rounding_power = 2
    active_opacity = 0.95
    inactive_opacity = 0.85

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    blur {
        enabled = true
        size = 2
        passes = 2
	     new_optimizations = true
        vibrancy = 0.1696
    }
}

animations {
    enabled = yes
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    animation = global,        1,     10,    default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     4.79,  easeOutQuint
    animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     4,     easeOutQuint, fade
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,    1,     1.94,  almostLinear, fade
    animation = workspacesIn,  1,     1.21,  almostLinear, fade
    animation = workspacesOut, 1,     1.94,  almostLinear, fade
    animation = zoomFactor,    1,     7,     quick
}

dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

master {
    new_status = master
}

misc {
    vfr = true
    font_family = Inter Regula
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}

#############
### INPUT ###
#############

input {
    kb_layout = us,br
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1
    accel_profile = flat

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# See https://wiki.hypr.land/Configuring/Gestures
gesture = 3, horizontal, workspace

###################
### KEYBINDINGS ###
###################

$mainMod = SUPER

bind = , Print, exec, hyprshot -m region
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, L, exec, pidof hyprlock >/dev/null || hyprlock
bind = $mainMod, C, killactive,
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod SHIFT, W, exec, ~/.config/hypr/scripts/wallpaper-select.sh
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, ~/.config/hypr/scripts/volume-notify.sh up
bindel = ,XF86AudioLowerVolume, exec, ~/.config/hypr/scripts/volume-notify.sh down
bindel = ,XF86AudioMute, exec, ~/.config/hypr/scripts/volume-notify.sh mute
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, ~/.config/hypr/scripts/brightness-notify.sh up
bindel = ,XF86MonBrightnessDown, exec, ~/.config/hypr/scripts/brightness-notify.sh down
bindel = ,XF86KbdBrightnessUp, exec, ~/.config/hypr/scripts/kbd-backlight-notify.sh up
bindel = ,XF86KbdBrightnessDown, exec, ~/.config/hypr/scripts/kbd-backlight-notify.sh down

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

bind = $mainMod SHIFT, P, exec, hyprpicker -a
bind = $mainMod SHIFT, T, exec, ~/.config/eww/scripts/toggle-sensors.sh

#clipboard
bind = SUPER, Z, exec, cliphist list | wofi --dmenu | cliphist decode | wl-copy

##############################
### WINDOWS AND WORKSPACES ###
##############################

windowrule {
  name = discord-float-center
  match:class = discord
  float = on
  center = on
}

#nautilus
windowrule {
  name = nautilus-float
  match:class = org.gnome.Nautilus
  float = on
}

# org.prismlauncher.PrismLauncher
windowrule {
  name = prism-launcher-float
  match:class = org.prismlauncher.PrismLauncher
  float = on
}

# blueman
windowrule {
  name = blueman-float
  match:class = blueman-manager
  float = on
}

# xdg-desktop-portal-gtk
windowrule {
  name = xdg-desktop-portal-gtk
  match:class = xdg-desktop-portal-gtk
  float = on
}

# org.gnome.Loupe
windowrule {
  name = loupe-float
  match:class = org.gnome.Loupe
  float = on
}

# bitwarden extension
windowrule {
   name = bitwarden-extension-float
   match:title = Extension: (Bitwarden Password Manager) - Bitwarden — Zen Browser
   float = on
}

windowrule {
	name = shimeji
	match:class = com-group_finity-mascot-Main
	
	float = true
	no_blur = true
	no_focus = true
	no_shadow = true
	border_size = 0
}

# calendar
windowrule {
   name = gnome-calendar-float
   match:class = org.gnome.Calendar
   float = on
}

# layerrule {
#    name = eww-bar
#    match:class = eww-bar
#    blur = on
#    ignore_alpha=1
#    blur_popups = on
# }

layerrule = blur on, ignore_alpha 0.3, blur_popups on, match:namespace eww-bar
layerrule = blur on, ignore_alpha 0.3, blur_popups on, match:namespace eww-menu
layerrule = blur on, ignore_alpha 0.3, blur_popups on, match:class wofi

